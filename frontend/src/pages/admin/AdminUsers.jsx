import { MOCK_DOCTORS, useApp } from "../../components/PrototypeApp.jsx";

export default function AdminUsers(){const {t,lang}=useApp();return(<div className="page"><div className="page-head"><div className="page-head-left"><h1>{t('doctorsList')}</h1><p>{t('doctorsSubtitle')}</p></div></div><div className="doctor-grid">{MOCK_DOCTORS.map(d=>(<div key={d.id} className="doctor-card"><div className="doctor-head"><div className="doctor-avatar">{d.avatar}</div><div><div className="doctor-name">{lang==='fa'?d.nameFa:d.nameEn}</div><div className="doctor-specialty">{lang==='fa'?d.specialty:d.specialtyEn}</div></div></div><div className="doctor-foot"><span className="doctor-foot-label">{t('totalVisits')}</span><span className="doctor-foot-value">{d.referrals}</span></div></div>))}</div></div>);}
